# Benchmark Registry

`registry.json` indexes the reviewed PR, Gold, and nightly protocols in `protocols/`. Protocols reference stable case IDs from `cases/catalog.json`; they do not embed case-specific generation rules.

Raw screenshots, traces, and repeated performance runs belong in CI artifacts or `UI_DISMANTLER_ARTIFACT_ROOT`. Commit only reviewed identities, final summaries, and explicitly frozen representative evidence.
