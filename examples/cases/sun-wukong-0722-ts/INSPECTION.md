# 人工查验

打开：

```text
examples/cases/sun-wukong-0722-ts/lib/examples/sun-wukong.html
```

建议依次检查：

1. 切换六段故事，图谱人物和关系线是否更新；
2. 点击孙悟空节点，人物详情面板是否打开；
3. 切换“演员对照”，点击版本卡片并关闭弹窗；
4. 切换“角色档案”；
5. 使用 desktop/tablet/mobile/tiny 尺寸检查布局。

自动截图位于 `artifacts/`，关键状态位于 `artifacts/scenarios/`。
